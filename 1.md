# HachiShifter next 分支交接记录

更新时间：2026-08-06（Asia/Tokyo）

## 当前状态

- 当前仓库：`/home/ubuntu/hjs/HachiShifter`
- 当前分支：`next`
- 工作树：干净
- 最新提交：`ae840de feat(next): add direct LLSM2 and melodic editor workflow`
- GitHub：`https://github.com/funnymdzz/HachiShifter`
- GitLab 镜像：`https://35.194.110.220/funnymdzz/HachiShifter`
- 构建策略：仅使用自托管 GitLab CI 云端编译；本地只下载无模型产物进行检查，不在 WSL2 配置或编译 JUCE 工程。

## 已完成内容

### 工程与构建

- `next` 分支已创建并推送到 GitHub。
- GitLab CI 已能分别生成 Linux 与 Windows 的无模型构建包。
- 最近一次通过的 GitLab pipeline：`36`。
- 无模型产物下载地址：
  - Linux：`https://35.194.110.220/funnymdzz/HachiShifter/-/jobs/artifacts/next/download?job=build_linux`
  - Windows：`https://35.194.110.220/funnymdzz/HachiShifter/-/jobs/artifacts/next/download?job=build_windows`
- 构建后应删除 `/tmp` 中的压缩包和解压目录，避免占用本地空间。

### JUCE 重构与界面

- `juce/` 为 C++/JUCE 前端和后端，保留 main 分支 JS/Rust 版的主要布局、钢琴卷帘、工具按钮和颜色风格。
- 默认主题色：紫色 `#7F69CA`、浅蓝色 `#CBCBFA`、黄色 `#F4C000`。
- 设置窗口包含界面、音频、算法、操作、工程导入五页；语言选择已合并到设置。
- 已加入五语言翻译（简体中文、繁体中文、日文、韩文、英文）；新增界面文本必须同步补齐五种语言。
- 已加入音频设备、采样率、缓冲区、推理后端/设备、GAME/FCPE/HiFi-GAN 路径、导入 Compose 默认值、快捷键和主题设置。
- 已加入素材设置、HJM CSV 保存、UTAU `oto` 导入/导出和素材管理器。
- 波形显示统一使用单声道折叠显示，即使源文件为立体声也只画一条波形。

### 音符与钢琴卷帘

- 当前轨道聚焦渲染，避免多轨道音符/滑块重叠；同一 MIDI 行重叠时使用紧凑子通道。
- 支持别名/发音标签可选显示、框选、Shift/Ctrl 多选、半音吸附、Alt 细调音分、边界拖动、双击量化和辅音边界对齐。
- 已实现连接/断开、音符分割、音高曲线保留幅度、调制/漂移/Attack/呼吸/张力/共振峰/音量参数。
- 导入 Melodyne 时读取音符、分界、Attack、音量、滑音、调制、漂移、共振峰、齿音/未发声信息、BPM 等项目数据。
- 已保存 Melodyne 5/3 的 Ghidra 工程和注释：
  - `reverse/melodyne5/melodyne-re.gpr`
  - `reverse/ghidra/Melodyne.gpr`
  - 说明：`reverse/melodyne5/README.md`、`reverse/notes/melodyne_pitch_engines.md`

### 算法与渲染

- 变调算法选项：`mld5`、`mld3`、`nsf-hifigan`、`WORLD`、`vslib`、`llsm2`。
- 拉伸算法选项：`melodyne-hybrid`、NSF 专属 `variable-mel-hop`、`loop`、`soundtouch`。
- GAME 默认模型为 `large`，性能模式可切换 `small`；GAME note 按音节处理，note 开头作为默认节奏对齐点，并支持静音/呼吸识别。
- NSF 可变 Mel Hop 路径已经保留：每个源时间片按目标时长计算 hop，拼接 Mel 后单次解码，后端标记为 `+variable-mel-hop-splice-first`。
- 已加入直接 `libllsm2` 路由（只接入算法，不包含完整 moresampler 前端）：
  - `juce/third_party/libllsm2`
  - `juce/third_party/ciglet`
  - `juce/src/backend/Llsm2Renderer.h/.cpp`
- 已修复 mld5 双重处理导致的回声路径，改为稳定单次渲染时钟；各后端在 MCP 中生成不同结果哈希。
- 已加入表达式与限制器、连接处去爆音和重叠片段增益控制的基础路径。

### MCP 与测试

- MCP 可访问项目快照、轨道/片段/音符、导入、渲染、导出、算法和设置等操作。
- 已通过：
  - `HachiShifterNext --inspect-settings`
  - `python3 juce/tests/mcp_smoke.py HachiShifterNext`
  - `python3 juce/tests/melodyne_import_smoke.py HachiShifterNext /mnt/c/Users/funny/Downloads/sample1/sample11.mpd`
- `sample11.mpd` 回归数据：BPM 158、1 轨、197 音符、197 个映射片段、12194 个时间点、4916 个音高点、745 个未发声点、5 个拉平音符。
- 现有 smoke 测试已验证六种变调后端和四种拉伸后端的输出差异，以及 `variable-mel-hop` 仅在 NSF-HifiGAN 下出现。

## 尚未完成的计划

### A. 渲染顺序需要成为独立可选项

最新需求是“保留原有可变 Mel 拉伸方案，并与先拉伸后拼接共存”。当前代码仍按“每个 Compose 片段独立处理后混音”的单一路径运行，`variable-mel-hop` 也仍是拉伸算法而不是独立的轨道处理顺序。

建议增加：

```cpp
enum class RenderOrder {
    processThenSplice,       // 每个片段先变调/拉伸，再拼接（现有路径）
    stretchSpliceThenPitch   // 先按时间拉伸并拼接整轨，再做一次变调
};
```

需要同步加入 `TrackData`、ProjectModel setter、HSPX 序列化/反序列化、MCP `set_track`/`project_snapshot`、导入默认设置、TrackList 标签、主界面和五语言翻译。默认保持现有 `processThenSplice`，不可破坏 `variableMelHop`。

建议的真正实现方式：

1. 对每个片段做无变调的时间处理，生成最终片段时长。
2. 在内存中按轨道时间线拼接/混音，保留片段 gain/fade/重叠信息。
3. 将整轨音符的 source/target F0、调制、漂移、共振峰等组合为轨道级曲线。
4. 对整轨缓冲区执行一次变调算法，并以轨道级缓存播放。
5. 普通音轨保持直接播放；`variableMelHop` 继续作为 NSF 的独立拉伸算法，在两种顺序中均可被选择。

优先扩展 `RenderService` 的内存缓冲区请求，不要写临时 WAV；同时更新 `AudioEngine::rebuildLoadedClips()`、缓存键和 `getNextAudioBlock()`。

### B. Melodyne 级渲染精度

- mld5 仍是模型无关的近似实现，距离 Melodyne 5 的高频抑制、鲁棒 F0、共振峰保持和极端高音伪影控制还有差距。
- mld3 目前是独立 period-transition clock 路由，尚未达到 Melodyne 3 原版的一致性。
- 转音、Attack 前后、音符结尾处仍需使用统一的 source-time/F0 映射，避免尾部跑调。
- 需要继续对 `sample1/vocal.wav`、`sample1/tt.mpd` 和 `sample11.mpd` 的指定时间段做 FCPE F0 对比。
- 导入工程默认应完全使用工程内数据；只有显式选择 `source_pitch=game+fcpe` 时才重新分析。

### C. 爆音、回声与音质

- 片段重叠处已有基础淡入淡出与限制器，但应进一步还原 Melodyne 的局部增益/渐变曲线，只在真实重叠或工程已有过渡处处理。
- 辅音/元音、齿音边界需要短窗相位连续和能量连续处理；避免全局自动 crossfade 改变工程原意。
- NSF-HifiGAN 仍可能出现小孩音、回声和高频过亮，需要检查模型输入时钟、mel 帧边界、解码一次路径和设备后端。
- mld5 高音沙哑/伪影需加入 Melodyne 风格高频选择性衰减，而不是简单全带宽滤波。

### D. 编辑器细节

- 个别导入音符滑块仍可能不显示或无法拖动；检查多轨删除后焦点轨道、空 contour、重叠音符 lane 分配和 hit-test。
- 音高曲线中的未发声/低置信区间应不绘制实线，以浅色片段表示；辅音以浅色、齿音用竖线标记。
- 连接状态需要读取 Melodyne 工程已有连接/滑音信息；默认不平滑未连接音符。
- 扳手模式播放应只播放当前原始片段，并将编辑应用到最终渲染。

### E. 性能与工程导入

- 大工程导入需继续采用分阶段进度条、惰性媒体读取和按轨道缓存，避免一次性复制全部 contour/PCM。
- 轨道级渲染加入后，使用有界缓存和可取消任务；进度条完成后保证立即播放。
- 多进程/多线程渲染需避免在音频回调中分配内存或等待锁。

## 下一台设备的开发流程

1. `git clone https://github.com/funnymdzz/HachiShifter`，切换 `next`。
2. 只修改 `juce/`、`reverse/`、测试和文档；不要在本地运行 CMake/JUCE 编译。
3. 提交并推送：

```bash
git checkout next
git add .
git commit -m "..."
git push origin next
```

4. 等待 GitLab 镜像 pipeline 完成后下载无模型包进行检查：

```bash
curl -ksSL 'https://35.194.110.220/funnymdzz/HachiShifter/-/jobs/artifacts/next/download?job=build_linux' -o /tmp/hachi-next.zip
unzip -q /tmp/hachi-next.zip -d /tmp/hachi-next
tar -xzf /tmp/hachi-next/HachiShifterNext-linux-x86_64-no-model.tar.gz -C /tmp/hachi-next
/tmp/hachi-next/HachiShifterNext --inspect-settings
python3 juce/tests/mcp_smoke.py /tmp/hachi-next/HachiShifterNext
python3 juce/tests/melodyne_import_smoke.py /tmp/hachi-next/HachiShifterNext /mnt/c/Users/funny/Downloads/sample1/sample11.mpd
```

5. 验证后清理 `/tmp/hachi-next*`。

## 重要约束

- 构建仅走 GitLab CI；GitHub Actions 状态不作为验收依据。
- 产物默认不携带 GAME/FCPE/HiFi-GAN 模型。
- 不删除 `juce/third_party/libllsm2`、`juce/third_party/ciglet` 或既有 Ghidra 工程。
- 每次新增 UI、MCP 字段或工程属性，都要补五语言文本、序列化兼容和 smoke 回归。

